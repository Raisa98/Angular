import { HomepagestyleDirective } from './homepagestyle.directive';

describe('HomepagestyleDirective', () => {
  it('should create an instance', () => {
    const directive = new HomepagestyleDirective();
    expect(directive).toBeTruthy();
  });
});
